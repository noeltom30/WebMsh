[build]
builder = "NIXPACKS"

[deploy]
startCommand = """
bash -c '
cd frontend && uv run dev &
cd ../backend && uvicorn app:main --reload --host 0.0.0.0 --port $PORT
'
"""

healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"